name: codespell

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  codespell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5.0.1
      - uses: codespell-project/actions-codespell@v2.2
        with:
          skip: ./.git,./go.sum
