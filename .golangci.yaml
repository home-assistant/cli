linters:
  disable:
    - errcheck
  enable:
    - dupword
    - gocritic
    - gofmt
    - gosec
    - nolintlint
    - perfsprint
issues:
  fix: true
  max-issues-per-linter: 0
  max-same-issues: 0
output:
  uniq-by-line: false
